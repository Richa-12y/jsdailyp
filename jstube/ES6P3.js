// Given an array rotate the array to the left by k steps, where k is a non-negative integer. Please note that this needs to be done in place, i.e., the same input array 
//itself needs to be rotated without creating a new array as a result.
// Input Format: An array and a number by which to rotate the array to the left
// Constraints: k is a non-negative integer
// Output Format: The rotated input array
// Evaluation Parameters
// Sample Input: [1, 2, 3, 4, 5, 6, 7], k=3
// Sample Output: [4, 5, 6, 7, 1, 2, 3]



function sum(a,b) {
  return a+b;   
}
console.log(sum(5,7))

let sum1=(b,c)=>{
     return b+c;
}
console.log(sum1(5,7))
